import fs from "fs"

class Helper {

  // dumpAndLogJson(data) {
  //   if (typeof data !== 'object' || Array.isArray(data)) {
  //     throw new Error('Data is not a valid JSON object');
  //   }
  //   try {
  //     fs.appendFile('../../dump/genrateWallet.json', JSON.stringify(data), (err) => {
  //       if (err) throw err;
  //     });
  //     console.log("JSON data dump complete")
  //   } catch (error) {
  //     console.log(error);
  //   }
  // }

}

export default Helper;
